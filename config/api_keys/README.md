# API Key Files

Store local Gemma API keys in:

```text
config/api_keys/gemma_api_keys.txt
```

Use one key per line, or comma-separated keys. Blank lines and lines starting with `#` are ignored.

These key files are ignored by git.
